#include <stdio.h>

int main() 
{
    // Single line comment
    /*
    Multiple Line comment
    */

   int x; //declaration
   x = 123; // initialization
   int y = 3; // declaration + initializationt

   int age = 21; 
   float gpa = 2.90;
   char grade = 'C';
   char name[] = "Bro" ; //Array of character so it still a string with B,r,o

   printf("what %d", 3); //check the format specifier in website
   scanf("%d", &x); // Input scan

}