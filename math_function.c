#include <stdio.h>
#include <math.h>

int main() {
    double A = sqrt(9);
    double b = pow(2,4); // first number is base
    int C = round (3.14); // gonna be 3
    int D = ceil (3.14); // use for round up
    int E = floor(3.99); // round down
    double F = fabs(-100); //absolute value for float, but also work for int, abs work the same but not for float
    //sin(), cos(), tan()
}