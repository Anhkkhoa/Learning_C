#include <stdio.h>

/*
int main() 
{
    //array - data structure to store many values of same data type

    double prices[] = {5.0, 10.0, 15.0, 25.0};

    char name[] = "Bro"; //array of each single character in that string

    printf("%lf", prices[0]); //using the same index as python

    double price2[10] = {5.0, 10.0, 15.0, 25.0}; //5 elements is empty
}
*/


int main()
{
    //2D array is just array with each element is an entire array

    int numbers [2][3] = {{1, 2, 3}, {4, 5, 6}}; // row first, column second, need to declare how much for each side

    /* calculate row and coumn num
    int rows = sizeof(numbers)/sizeof(number[0]);
    int columns = sizeof(number)/sizeof(numbers[0]);
    */

}